# 3.5.0 粉笔错题本与云端同步

日期：2026-09-21。该记录用于继续维护当前工程，不包含真实题目、个人答案、账号凭据或扫码令牌。

## 本轮范围

在「错题复盘」中增加「粉笔错题本」，保留原来的训练复盘。新错题库独立于训练统计、空间码同步和训练记录存储，不把题目导入或复习次数伪装成训练成绩。

连接采用每名使用者分别扫码的云端方案。网站接入独立于 ChatGPT/Codex 插件；浏览器不提交粉笔密码、不接收粉笔 Cookie。云端在有效授权会话下只读当前行测错题、材料、解析、知识点和原作答。每次成功同步后安排 6 小时后的下一次更新，人工同步可提前排队。遇到登录或验证要求时保留旧数据并要求重新扫码，限流则退避重试。

复盘支持先作答再揭示、原答案对照、笔记和间隔复习。再练为 10 分钟，模糊为 1 天，掌握按 1、3、7、14、30、60 天推进。原题来源更新与笔记、复习记录使用不同时间字段合并；并发复习事件按稳定标识保留。

## 工程入口

- `src/mistakes.ts`：来源导入、批次材料映射、复习计划、笔记和云端合并。
- `src/MistakesView.tsx`、`src/mistakes.css`：错题列表、筛选、作答、解析和复习界面。
- `src/MistakesWorkspace.tsx`、`src/mistakeApi.ts`：扫码状态、账号隔离、云端拉取与进度同步。
- `src/mistakeStorage.ts`：按账号隔离的 IndexedDB；未登录导入使用单独存储入口。
- `supabase/functions/fenbi-cloud/index.ts`：鉴权、一次性扫码事务、进度合并和同步任务。
- `supabase/functions/fenbi-cloud/provider.ts`：受限官方主机上的只读协议、Cookie 作用域和来源响应检查。
- `supabase/schema/fenbi_cloud.sql`：服务端专用账号、会话、扫码表，Vault 密钥读取和定时任务。

当前仓库没有单独的 `PROJECT_HANDOFF.md`；README 与本操作记录作为交接入口，避免出现重复且不一致的维护说明。

## 已验证行为

截至本记录写入时，全部 40 项回归测试通过，包含数据模型 13 项、云端处理器 9 项、粉笔协议 9 项及原训练功能。测试只使用合成题目、合成账号和拦截网络的模拟服务，没有真实登录或写入用户的粉笔数据。

数据测试覆盖题号去重、批次内材料映射、未知模块保留、多种答案格式、重复导入保留笔记与复习、完整性声明核对、缺失题目保留、时间安排、跨设备合并、恶意结构归一化、JSON 往返，以及本机写入失败保留旧备份。

云端行为测试覆盖未授权拒绝、未知或过期会话、按 token 绑定账号、拒绝未知题号与题干注入、进度版本冲突重试、清空笔记同步，以及两个同时到达的扫码请求只能创建一枚会话。已消费的扫码事务重放被拒绝；Cookie 和临时扫码状态在数据库请求中为密文，篡改密文会在访问粉笔前失败。

对已有私有导出的结构验收仅在内存完成：题号唯一、批次材料映射成功、无空题干或未分类题，网站备份导出后可无损再导入。私人导出没有复制进本工程，也不作为测试样本或部署资源。

对前端账号工作区的只读审查确认：账号与令牌变更会重建工作区，退出会中止旧请求，服务端返回的账号标识需与当前工作区一致，进度响应按当前本机状态合并。审查发现并回读了三项修复：匿名导入在文件读取后重新合并最新本机进度；本机读取失败时不进入正常写入与云同步流程，并提供重试；题目阅读页区分真正编辑过的笔记与云端更新，避免离开页面时把旧文字覆盖回云端。代码回读与浏览器故障回归应分别记录，不能相互替代。

## 仍待实际验证

以下项目不能由测试通过、发布成功或生成二维码推定已经完成：

1. 用户用自己的粉笔 App 完成真实扫码，并取得有效身份与可用的服务端 Cookie。
2. 部署环境读取该账号的行测错题树、题目、材料与原答案，核对完整性。
3. 两台设备登录同一账号后，笔记与复习进度真实同步；不同账号之间不出现彼此记录。
4. 定时任务针对真实账号在网页关闭时继续读取；会话过期、停止同步与重新连接的真实行为。
5. 正式域名的电脑、手机布局和受影响入口，以及最终前端构建、云端部署与发布状态。

最终验收时追加真实结果与限制，不把上述待验项直接改成笼统的“全部打通”。扫码依赖粉笔当前网页协议，不应称为官方第三方 OAuth 或保证会话永久有效。协议证据与边界见 [粉笔网页登录协议](../design/fenbi-auth-protocol.md)。

## 运维与数据边界

生产源码以本 Git 工程为准。前端和 Edge Function 分别部署；更新网页不会自动更新云端函数或数据库。实际发布、函数部署、权限与定时任务状态以当次操作回读为准。

`xc_fb_accounts`、`xc_fb_sessions`、`xc_fb_logins` 均仅供服务端操作；不能给匿名客户端添加直读策略。粉笔会话的 AES-GCM 密钥来自 Vault，运行时密钥只允许服务角色读取，不能放进前端变量、日志或截图。站内令牌只存哈希。原训练 `xingce_sync` 的业务数据不属于本轮错题同步范围。

源数据写入与进度写入独立，worker 通过限时租约与条件写入防止过期任务覆盖重连或停止同步后的状态。源批次未完整返回时保留上次成功结果。登录失效与限流分别处理，不通过伪造设备或绕过验证维持读取。

退出网站账号只注销当前会话并隐藏当前页面记录；停止自动同步会移除云端粉笔会话并停止后续读取，已有错题和复盘保留。浏览器中的各账号缓存也仍在本机；清理或迁移前分别导出训练备份与错题备份。

## 发布前浏览器与云端验收

- 本地实际构建在 1360 × 980 与 390 × 844 检查；已查看浅色、深色截图，无横向溢出。Browser 插件未提供，采用已有运行库的 Playwright 与独立无头 Edge；未使用真实浏览器的登录数据。
- 合成错题导入、材料显示、答案默认隐藏、多选、揭示解析、笔记保存、评级安排、刷新恢复通过；恶意 HTML 不执行，控制台无相关错误。
- A/B 合成账号切换、退出后旧数据隐藏、401 返回登录、仅进度上传且不传题源、各账号 IndexedDB 隔离通过。
- 云端函数已部署，health 200，未授权 account/notebook/worker 均 401；新数据表 RLS 开启且匿名和普通认证角色不能直接读取。
- 网页已实际从云端取得二维码并进入等待扫码；未代替用户完成手机确认。每分钟云端任务已有连续 HTTP 200、processed=0 的实际回执，证明无人登录时调度链路正常；真实账号同步及关闭网页后的读取仍待扫码后验证。
- 过期会话清理任务已启用并回读；每天北京时间 03:20，仅清理临时扫码事务与已过期的网站会话，不清理账号、错题和复盘。
- 前端构建、服务端严格类型检查、依赖审计（零已知漏洞）通过。真实生产版本部署状态以最终发布追加记录为准。

## 免费额度与流量控制

2026-09-21 只读检查：整个 Postgres cluster 约 27.6 MB。619 题本机合成转换后的完整 JSON 约 1.83 MB；这只是体积计算，新账号表当时为 0 行，不能当作已验证真实云端错题读取。50 份类似题库正文约 91 MB，另需预留复盘历史、索引、临时同步游标与数据库更新空间。图片仅引用已允许的粉笔图片地址，不复制到 Storage。

Supabase Free 当前为每项目 500 MB 数据库、每组织每月 5 GB 非缓存出站与 50 万 Edge 调用。若 50 人每天完整下载两次，未压缩题目正文一个月即约 5.5 GB，因此人数不能单凭存储估算。官网依据：[账单说明](https://supabase.com/docs/guides/platform/billing-on-supabase)、[出站口径](https://supabase.com/docs/guides/platform/manage-your-usage/egress)。

网页轮询使用不含完整题库的服务端状态视图：版本无变化不下载题库，仅复盘版本变化读取进度，题源版本变化或首次进入才下载完整题库。已通过独立浏览器请求计数验证。网站当前不是容量或并发压力测试的结论；小规模试用后应根据实际月流量和函数调用数评估扩展。

- 补充带宽验证：相同题源内容保留原版本，仅检查时间变化不触发整本重下；轻量状态与进度接口的账号绑定也加入测试，当前共 40 项通过。前端空闲检查只取状态、仅复盘变化只取进度，真实题源变化才取整本。
