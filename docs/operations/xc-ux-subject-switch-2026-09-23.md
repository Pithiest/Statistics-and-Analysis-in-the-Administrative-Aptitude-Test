# 行测交互与双科目入口（2026-09-23）

## 本轮范围

- 总览主操作跟随统计来源：粉笔自动记录进入「全部练习」，手动来源进入「录入训练」；无同步练习时用实际状态描述，不暗示已经录入。
- 复盘入口优先展示可处理内容，并保留「全部练习」作为相邻入口。粉笔练习按每个题型组自己的正确数判断是否待复盘。
- 统计来源、历史同步、离线缓存、保存与同步失败状态使用清楚的提示；保留本机先保存、空间码同步及已有记录。
- 行测侧栏和手机页头提供前往 `gz.pithiest.cn` 的普通链接；公专站对应提供返回行测的链接。页面注明两科目记录各自保存，不传递空间码、会话或训练内容。

## 合并评估

| 方案 | 收益 | 工作量与主要风险 | 判断 |
| --- | --- | --- | --- |
| 双站入口互通 | 两科目可以直接往返，沿用各自已验收的功能 | 低；重点验收双向链接、手机布局和各站版本 | 本轮实施 |
| 共享界面规范 | 入口、状态和设置用语逐步一致 | 中；两站页面结构不同，仍需分别构建、部署和维护 | 入口稳定后按真实使用问题推进 |
| 单域名与统一账号、数据 | 可能提供跨科目总览和一次登录 | 高；需新的身份映射、明确授权、服务端会话改造、两个加密空间和浏览器本机记录迁移、冲突处理、离线缓存及 PWA 作用域调整、提醒链接迁移和回滚演练 | 暂不直接合并 |

行测使用 `xingce_sync`、独立的粉笔云端表与会话；公专使用 `gz_study_spaces`、`gz_fb_*` 和自己的同源代理。浏览器本机数据被域名隔离；即使后端位于同一 Supabase 项目，也不能直接互认登录或无损读取另一站记录。下一步只有在用户确需跨科目总览时，才先设计只读聚合、身份与授权模型；全量迁移需分别导出、合成账号演练、逐项核对与可回退方案。

## 验证记录

本机生产构建已在隔离浏览器以合成数据检查 320、390、768、1440 像素；检查了两站科目入口、手机导航与横向溢出。行测另检查了粉笔空档案、已有练习及详情、手动来源、离线缓存、账号状态失败、复盘切页、深色主题和减少动态效果；未使用真实训练或同步数据。

行测 52 项测试、类型检查和生产构建通过；GitHub PR #1 的验证工作流通过。Vercel 预览部署 `dpl_FiJqALB6hUu84oLv5o5GgcxQbyaR` 已就绪，预览受登录保护，使用授权 CLI 回读入口、主脚本、样式和 Service Worker 为 HTTP 200，资源哈希与本机验收构建一致。

行测 PR #1 合并提交为 `7b0cd880ef6aa381fc0ecc36ebc061dc3dec438b`，Vercel 生产部署 `dpl_81g2j95uVC4sTGpmM9XhdSP3XUWn` 为 Ready，正式域名 `xc.pithiest.cn` 指向该部署。正式站在隔离浏览器的 390、1440 像素加载和刷新正常，无页面错误；主脚本 `index-BIwXQhCE.js`、样式 `index-COSSQNQu.css` 与 Service Worker 返回 HTTP 200。手机入口实际跳转到了 `gz.pithiest.cn`。

公专的反向入口保存在独立检出 `/Users/pithiest/gongzhuan-study-ux` 的提交 `fb6102383ba150c2a657a767da412301fb10093a`，交由现行公专工程整合发布；不能将本机的公专布局检查当作公专正式域名的反向入口验收。两站真实账号跨设备与长期网络表现也未在本轮验证。

## 2026-09-23 PWA 缓存升级复核

复核发现：旧 Service Worker 会在入口脚本或样式未能预缓存时仍切换版本，并在激活时删除此前缓存；加载恢复按钮也会清除全部站点离线资源。两处都不删除 localStorage/IndexedDB 训练数据，但会削弱离线回退。已将新缓存隔离为 v13，要求入口 HTML 与入口静态资源全部预热成功后才允许切换，并保留当前版本及上一代缓存；加载恢复改为直接重载，不再清空 CacheStorage。

本地合成 Service Worker/CacheStorage 测试覆盖升级资源失败、成功激活、上一代资源离线回退，以及加载壳重试不删除缓存或合成记录。63 项测试、类型检查与生产构建通过。没有访问真实训练数据、空间码或线上服务；浏览器安装与隔离浏览器调用未能完成，因此真实浏览器刷新、安装提示和离线界面本轮未验收。改动尚未部署。
