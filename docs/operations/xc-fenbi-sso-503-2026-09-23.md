# XC 共享粉笔登录生产 503 调查

调查日期：2026-09-23。只使用匿名合成请求；没有扫码、读取二维码内容、登录令牌或真实学习数据。

## 部署范围

- 生产部署：`dpl_He1Yx5RwAxfRnxuVCmjcy2otmWho`，状态 `READY`，生产域名 `xc.pithiest.cn`。
- 部署提交：`2ac87d9997e992aa9bb3ac69a35dcca708246652`（合并 PR #2），函数区域 `iad1`。
- 部署源代码中的代理为 `api/sso.js`，固定请求 Supabase `gz-fenbi` 函数的 `/login/start` 路径。

## 首个合成生产失败

- 开始：`2026-09-23T02:42:20.396Z`；结束：`2026-09-23T02:42:21.475Z`。
- 请求：`POST /api/sso?path=start`，`Content-Type: application/json`，同源 `Origin: https://xc.pithiest.cn`。
- 请求体：JSON 空对象 `{}`；应用类型为 object；2 字节；没有 Cookie 或 Authorization。
- HTTP 状态：`503`。
- `x-vercel-id`：`sin1::iad1::zj7bh-1790131341032-69dacec342da`。
- 原始 HTTP 响应体：`{"error":"共享登录暂时不可用，请稍后重试。"}`。

这是处理器最外层 `catch` 的通用错误文本，不是 `/login/start` 分支本身的错误文本。因此可确认请求进入处理器后出现了异常，但此响应没有包含底层异常。

## 合成对照

- 本机直接请求同一 Supabase `gz-fenbi/login/start`：`2026-09-23T02:44:38.349Z` 至 `02:44:40.557Z`，HTTP `200`。仅确认返回字段名，二维码正文和挑战值未输出或保存。
- 本机直接运行 `handleSsoRequest` 并请求同一上游：`2026-09-23T02:44:40.616Z` 至 `02:44:42.179Z`，HTTP `200`。响应字段值未输出或保存。
- 总控提供的基线：匿名 `session` 返回 `401`、非法 `start` 返回 `400`；同一扫码入口在 `gz.pithiest.cn` 返回 `200`。

## 运行时日志与结论边界

- 已针对该部署、503、`/api/sso` 和上述 Vercel 请求 ID 查询运行日志及错误聚合。
- Vercel API 在返回日志前拒绝访问（`403`），并提示需要重新授权 `pithiest-s-projects` 团队范围。部署元数据可读，运行时日志不可读；这不能视为“没有日志”。
- 当前代码的最外层异常处理不记录异常名称或堆栈。现有证据将故障范围缩小到请求校验之后的异常路径，但不能区分请求体读取、信号处理或上游网络调用，也不能据此提交推测性根因。
- 待运行时日志权限恢复后，读取同一请求的日志，确认底层错误及其发生阶段，再实施并验证修复。

## 后续处置

随后在与生产入口一致的双参数调用中复现：运行时传入的第二个 `context` 参数被误当作测试用的请求函数。PR #4 把生产导出改为单参数包装，合入部署后，同源匿名 `/api/sso?path=start` 返回 HTTP 200，二维码字段结构有效。生产时的原始底层异常日志仍未取得，因此上述复现是对根因的有力解释，不等同于读到了当时的生产堆栈。真实粉笔 App 确认和跨站账号闭环另行验收。
