# xc.pithiest.cn 恢复与验收记录

检查时间：2026-09-21 20:25（Asia/Shanghai）

后续更新：用户已确认网站恢复。本记录从工程根目录归档到 `docs/operations/`；完整工程现位于 `/Users/pithiest/good_things`。下述接口与部署结果是恢复检查时的证据，不能代替后续界面改动的发布验收。

项目现役入口：[README.md](../../README.md)；协作规则：[AGENTS.md](../../AGENTS.md)。

## 已完成的恢复

- Supabase 项目「行测统计数据同步」（atwsraivphybkfmyeubd）原状态为 INACTIVE。
- 调用恢复后经过 COMING_UP，最终为 ACTIVE_HEALTHY。
- 未改动域名绑定、Vercel 部署、数据库结构或用户记录。
- 原项目使用 Supabase Free 计划。官方说明 Free 项目可能因低活动暂停；本次没有取得暂停原因日志，不能将自动暂停原因视为已证明。

## 验收

- xc.pithiest.cn CNAME 仍指向 Vercel：0f28bb70014ca87f.vercel-dns-017.com。
- HTTPS 首页返回 200；HTTP 返回 308 跳转至 HTTPS。
- 在本机现有网络路径及绕过环境代理的路径下，HTTPS 首页都返回 200。
- 直接请求首页、JavaScript、CSS、Service Worker 均为 200。
- Edge 实际打开训练总览和同步设置，刷新后仍正常渲染。
- Supabase 同步表只读查询成功。表中共 8 行，其中 1 行为构建源码，另外 7 行为非源码数据行；这不等于训练记录条数。
- 使用网站现有客户端公开密钥做 limit=0 的只读同步 API 验证，返回 200；对应 CORS 来源正确为 https://xc.pithiest.cn。
- Supabase Auth 健康接口返回 200。网站实际跨设备功能采用空间码同步，并非账号登录流程。
- GitHub HEAD 553357aaee2d50f20136fef3e5e654d432c9a1b7 对应 Vercel Production 发布记录为 success（2026-06-05）。

## 源码

- 本机目录：/Users/pithiest/good_things（恢复时先克隆到其下的 `xingce-statistics`，后依用户要求连同 `.git` 完整移至工程根目录）
- 仓库：https://github.com/Pithiest/Statistics-and-Analysis-in-the-Administrative-Aptitude-Test
- 发布分支：行测的统计与分析
- 恢复检查结束时已克隆仓库，工作区干净；当时未执行构建、安装依赖或发布新部署。
- 恢复时本地验证使用 `npm run build:local`，普通 `build` / `dev` 会先从 Supabase 恢复源码。这是历史行为；后续构建入口以现役 [README.md](../../README.md) 和 `package.json` 为准。
- 工程整理前备份：`.local-backups/xingce-before-ui-20260921.tar.gz`，仅在本机保留；旧截图完整归档至 [docs/reference/xingce-data-preview.png](../reference/xingce-data-preview.png)。

## 验证边界

- 用户报告原故障是另一台设备 Wi-Fi 上整个网址连接超时。
- 本机没有复现该超时，且前台初次检查时已能返回 200，因此不能把过去的整站超时归因于 Supabase 暂停，也不能声称恢复后端解决了那台设备的网络问题。
- 未取得真实空间码，也未读取、解密或改写用户训练内容；跨设备实际数据合并和上传没有执行验收。
- 本次未验证原来发生故障的设备及 Wi-Fi 路径。

官方暂停说明：https://supabase.com/docs/guides/platform/free-project-pausing
