# 行测训练统计系统：工程协作入口

本目录 `/Users/pithiest/good_things` 是 `xc.pithiest.cn` 的完整现役工程。开始维护先读 [README.md](README.md)，按需要查看 `docs/operations/` 的相关记录。本文件是项目协作规则的单一来源；旧 `AGENT.md` 仅作入口兼容。

## 范围与数据

- 保留本机优先保存、空间码同步、录入、计时、快捷模板、诊断、复盘、台账和导入导出能力。
- 保存先写本机，云端失败不得阻塞录入。保留存储读写异常处理和旧记录归一化兼容。
- 普通切页、筛选、弹层、主题切换和计时器操作不触发上传。持久数据真实变化后才标记待同步；本机变化合并 10 秒上传，常规拉取至少间隔 2 分钟。
- 不在日志、截图、文档或提交信息中记录真实空间码、访问令牌或训练内容。不要为了界面测试操作真实同步数据。
- 保留未提交修改、本机备份与唯一历史资料。源码、构建产物、浏览器数据和云端数据分别判断，不能互相当作备份。

## 统计口径

- 模块和小题型以 `src/model.ts` 为准。旧数据导入经过归一化，不把言语、判断、资料、数量或常识误判为全模块测试。
- 百分比和模块健康度在 `0–100` 内；健康度综合正确率、配速、样本量、训练新鲜度和复盘完成度。
- 无训练日期的正确率与配速使用 `null`，图表连接已有训练点，不把无样本画成 0。
- 常识诊断保留政治理论、法律常识、科技人文、经济管理、综合卷/混刷，即使暂无样本。
- 首页近期对比保持最近 7 天与此前 7 天的可解释口径；改动口径时同步说明与相应测试。

## 界面与稳定性

- 保持数据工作台的有效信息：今日行动、核心信号、趋势、模块健康矩阵、训练编排、复盘与错因。布局以本次用户接受的设计为准，视觉改动不能删减功能。
- 诊断支持模块、题型、时间范围和小题型健康表；桌面台账便于比较，手机记录便于扫描，不能横向溢出。
- 署名 `Pithiest巨献` 保持低调，放在侧栏或设置页角落。
- 动效服务切页、交互反馈、状态与图表进入，保留减少动态效果偏好，不增加首屏等待或干扰输入。
- 维持单一主入口与重模块按需加载；首帧不被本机存储读取阻塞。加载壳与正式主题衔接，缓存更新不清除个人训练记录。

## 构建与恢复

- Git 工作目录是当前源码的唯一维护入口。使用 `package.json` 中的实际命令，以 `npm test`、`npm run typecheck` 和 `npm run build` 验证相应变动；浏览器检查使用真实构建结果。
- `dev`、`build`、`build:local` 直接使用本地源码；GitHub CI 只读验证，不恢复或推回云端源码。具体命令、运行版本与部署配置见 [README.md](README.md)。
- 历史源码包、`scripts/prepare-source.mjs` 与 `scripts/build-source-payload.mjs` 保留作恢复参考。`npm run source:restore` 是显式覆写入口，恢复前保留现有改动；不根据旧记录自动向云端写入源码包。
- 保留 `src/`、`public/`、`index.html`、构建配置、测试及有效本地 `deploy-source.json`。`dist/` 可以重建，预览使用期间不要删除。

## 验证与交付

- 检查本次受影响行为及必要依赖。统计或同步变动执行相关测试；界面变动实际打开桌面与手机尺寸，核对受影响流程、键盘操作、深浅主题及控制台错误。
- 测试与构建通过后，只有新修改、失败或未解疑点才扩大或重复检查。不要为简单可逆的文档修改补造测试。
- 发布后验证 `https://xc.pithiest.cn` 的真实入口、刷新、加载资源与受影响页面；区分本地生成、验证通过、已推送、已部署与线上验收。
- 已确认事实写进 README 或对应操作记录；历史记录保留日期和验证边界。部署成功不等于所有设备和网络均通过验收。
